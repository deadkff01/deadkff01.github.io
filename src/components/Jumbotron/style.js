import styled from '@emotion/styled/macro'
import { tronBlue } from '../../consts'

const Jumbotron = styled.div`
  color: #58beff;
  border: 1px solid ${tronBlue};
  font-size: 22px;
  padding: 40px;
  margin: 0px 10px 60px 10px;
  border-radius: 10px;
  box-shadow: 0px 0px 3px ${tronBlue};
`
export default Jumbotron
